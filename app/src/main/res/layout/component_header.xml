<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cl_header"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:id="@+id/ll_header"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:background="@drawable/bg_header_pattern"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Logo -->
        <ImageView
            android:id="@+id/iv_header_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/default_spacing"
            android:adjustViewBounds="true"
            android:contentDescription="@string/texi_logo"
            android:scaleType="fitCenter"
            android:src="@drawable/img_header_logo" />

        <Space
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Menu icon -->
        <ImageButton
            android:id="@+id/ib_header_menu_icon"
            style="@style/DefaultIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/default_spacing"
            android:background="@null"
            android:contentDescription="@string/open_menu"
            android:src="@drawable/ic_header_hamburger_menu" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>